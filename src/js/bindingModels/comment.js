var BindComment = (function () {
    function Comment(content, author, email, id) {
        this.content = content;
        this.author = author;
        this.email = email;
        article = {
            _type: 'KinveyRef',
            _id: id,
            _collection: 'articles'
        }
    }

    return Comment;
}());